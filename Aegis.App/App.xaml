<Application x:Class="Aegis.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Aegis.App.Converters"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             StartupUri="MainWindow.xaml">

    <Application.Resources>

        <!-- ===================== -->
        <!-- Core Colors            -->
        <!-- ===================== -->
        <SolidColorBrush x:Key="MenuBackground" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="MenuHoverBackground" Color="#FF333333"/>
        <SolidColorBrush x:Key="MenuSelectedBackground" Color="#FF444444"/>
        <SolidColorBrush x:Key="MenuForeground" Color="White"/>
        <SolidColorBrush x:Key="ContentBackground" Color="#FF121212"/>

        <!-- ===================== -->
        <!-- DataGrid Colors        -->
        <!-- ===================== -->
        <SolidColorBrush x:Key="GridBackground" Color="#FF121212"/>
        <SolidColorBrush x:Key="GridHeaderBackground" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="GridRowBackground" Color="#FF1A1A1A"/>
        <SolidColorBrush x:Key="GridAltRowBackground" Color="#FF202020"/>
        <SolidColorBrush x:Key="GridBorder" Color="#333333"/>
        <SolidColorBrush x:Key="GridSelection" Color="#FF2F2F2F"/>
        <SolidColorBrush x:Key="GridText" Color="White"/>
        <SolidColorBrush x:Key="GridMutedText" Color="#AAAAAA"/>

        <!-- ===================== -->
        <!-- IntegerUpDown (Global) -->
        <!-- ===================== -->
        <Style x:Key="DarkIntegerUpDownStyle"
               TargetType="{x:Type xctk:IntegerUpDown}">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type xctk:IntegerUpDown}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="PART_TextBox"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{TemplateBinding Foreground}"
                                         VerticalContentAlignment="Center"
                                         HorizontalContentAlignment="Right"/>

                                <Grid Grid.Column="1">
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>

                                    <RepeatButton Grid.Row="0"
                                                  Content="▲"
                                                  FontSize="10"
                                                  Foreground="#AAA"
                                                  Background="#333"
                                                  BorderBrush="#555"/>

                                    <RepeatButton Grid.Row="1"
                                                  Content="▼"
                                                  FontSize="10"
                                                  Foreground="#AAA"
                                                  Background="#333"
                                                  BorderBrush="#555"/>
                                </Grid>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border"
                                        Property="BorderBrush"
                                        Value="#777"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Border"
                                        Property="BorderBrush"
                                        Value="#888"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== -->
        <!-- Menu Item              -->
        <!-- ===================== -->
        <Style x:Key="MenuItemStyle" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource MenuForeground}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{StaticResource MenuHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{StaticResource MenuSelectedBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===================== -->
        <!-- Button Styles          -->
        <!-- ===================== -->
        <Style x:Key="AegisButton" TargetType="Button">
            <Setter Property="Height" Value="46"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2B2B2B"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3A3A3A"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#1F1F1F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#1E1E1E"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#333"/>
                                <Setter Property="Foreground" Value="#777"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- ===================== -->
        <!-- TextBox                -->
        <!-- ===================== -->
        <Style x:Key="AegisTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1C1C1C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>

    </Application.Resources>
</Application>


